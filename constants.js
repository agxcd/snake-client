const IP = "10.0.2.15";
const PORT = "50541";

// player's name/initials
//movement keys (W, A, S, D) could be added to constants like like MOVE_UP_KEY
//key mappings for messages

module.exports = {
  IP,
  PORT,
};
